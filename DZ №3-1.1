#include <iostream>
#include <cmath>
#include <iomanip>

using namespace std;
double f(double x) {
	double y;
	y = sqrt(4 * x / sqrt(3));
	return y;
}

int main()
{
	double s;
	cin >> s;
	cout << f(s) << endl;
}
