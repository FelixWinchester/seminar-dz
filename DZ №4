#include <iostream>
#include <cmath>
#include <iomanip>
using namespace std;
int main() {
	double x, c;
	cin >> x;
	c = cosh(pow(x,-7)) + ((log(2 * x) / log(7. / 8)));
	cout << fixed << setprecision(5) << c - int(c) << endl << int(c);
	return 0;
}
